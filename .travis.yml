language: java
jdk:
  - openjdk8
cache:
  directories:
    - "$HOME/.m2"
sudo: required
services: docker
notifications:
  email:
    - pelayochristian.dev@gmail.com
branches:
  only:
    - master
script:
  - sh travis_script/maven_install.sh
  - sh travis_script/deploy_to_docker_hub.sh
#  - sh travis_script/deploy_to_cloud.sh