language: java
jdk:
  - openjdk8
cache:
  directories:
    - "$HOME/.m2"
sudo: required
services: docker
notifications:
  email:
    - pelayochristian.dev@gmail.com
branches:
  only:
    - master
script:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - mvn install