server:
  port: 8888
spring:
  cloud:
    config:
      discovery:
        enabled: true
      server:
        git:
          uri: https://github.com/pelayochristian/config-repo
          searchPaths: gatewayserver,newsservice
          username: pelayochristian
          password: chan@082417
          default-label: master
        encrypt:
          enabled: false
